name: 'healthcare_dbt'
version: '1.0.0'
config-version: 2

profile: 'healthcare_dbt'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  healthcare_dbt:
    staging:
      +schema: silver
      +materialized: view
    marts:
      +schema: gold
      +materialized: table
